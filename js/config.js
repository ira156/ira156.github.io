/* 赵梓池工作台 · 跨设备同步配置
 * 在此填入你的 Supabase 项目信息。
 * anon key 是「公开安全」的（设计上可放在前端），放心写入。
 * 不填则自动回退为「仅本机存储」，不影响现有功能。
 */
window.ZC = {
  SUPABASE_URL: '',   // 例：https://xxxx.supabase.co
  SUPABASE_ANON: ''   // 例：eyJhbGciOiJIUzI1Ni...（Project API keys 里的 anon public）
};
